# Environment / secrets
.env
.env.*
*.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Node
node_modules/
dist/
build/

# OS / editor
.DS_Store
.idea/
.vscode/

# Logs
*.log

# Local agent data store
*.db

# Keep the example env committed (overrides .env.* above)
!.env.example

# Team mapping contains names/handles (PII) — keep local
team.json
!team.example.json

# Generated daily digests (contain names/work summaries — PII)
digests/

# Telegram MTProto session (account login — never commit)
*.session
*.session-journal
